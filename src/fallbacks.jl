
function coefficients(a...)
  return AbstractAlgebra.coefficients(a...)
end

function monomials(a...)
  return AbstractAlgebra.monomials(a...)
end

function terms(a...)
  return AbstractAlgebra.terms(a...)
end

function leading_coefficient(a...)
  return AbstractAlgebra.leading_coefficient(a...)
end

function leading_monomial(a...)
  return AbstractAlgebra.leading_monomial(a...)
end

function leading_term(a...)
  return AbstractAlgebra.leading_term(a...)
end

